/**
 * Persistance locale + partage.
 *
 * ┌──────────────────────────────────────────────────────────────────────────┐
 * │  STUB — à implémenter (brief §9.8, tests 11-12).                           │
 * │  - Dexie (IndexedDB) : sauvegarde/chargement des usines de l'utilisateur.  │
 * │  - Export/import JSON.                                                      │
 * │  - Sérialisation d'état → URL compressée pour le partage (round-trip).     │
 * └──────────────────────────────────────────────────────────────────────────┘
 */

export {};
